{
  "name": "typescript-azure-functions",
  "description": "",
  "version": "0.1.0",
  "scripts": {
    "build": "rimraf dist && tsc && cp-cli HttpTrigger/function.json dist/HttpTrigger/function.json",
    "start": "cd dist && func start",
    "watch": "tsc -w",
    "func-start": "func start",
    "deploy": "chmod +x ./deploy.sh && ./deploy.sh"
  },
  "devDependencies": {
    "@azure/functions": "^1.0.1-beta2",
    "azure-functions-ts-essentials": "^1.3.2",
    "case": "^1.6.1",
    "cp-cli": "^2.0.0",
    "rimraf": "^3.0.2",
    "tslib": "^2.1.0",
    "typescript": "^3.3.3"
  }
}