{
  "name": "typescript-azure-functions",
  "description": "",
  "version": "0.1.0",
  "scripts": {
    "build": "rm -rf dist && tsc && cp HttpTrigger/function.json dist/HttpTrigger",
    "start": "yarn --cwd ./dist func-start",
    "watch": "tsc -w",
    "func-start": "func start",
    "deploy": "chmod +x ./deploy.sh && ./deploy.sh"
  },
  "devDependencies": {
    "@azure/functions": "^1.0.1-beta2",
    "azure-functions-ts-essentials": "^1.3.2",
    "case": "^1.6.1",
    "typescript": "^3.3.3"
  }
}